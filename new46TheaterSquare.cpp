#include <iostream>
#include <cmath>
using namespace std;

int main()
{
  long long n{},m{},a{};
  cin>>n>>m>>a;
  long long int x=ceil(double)n/a);
  long long int y=ceil((double)m/a);
  long long int tot=x*y;
  cout<<tot<<endl;

  return 0;
}

